# Runtime dependencies (mirror of pyproject.toml [project.dependencies]).
# Kept for hosts that build from requirements.txt (Render, Railway, Fly).
# Install editable + dev extras locally with:  pip install -e ".[dev]"

fastapi>=0.111,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
sqlalchemy[asyncio]>=2.0.30,<2.1
asyncpg>=0.29,<1.0
alembic>=1.13,<2.0
greenlet>=3.0
httpx>=0.27,<1.0
tenacity>=8.4,<9.0
apscheduler>=3.10,<4.0
python-dateutil>=2.9
